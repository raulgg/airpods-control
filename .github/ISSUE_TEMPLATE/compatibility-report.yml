name: Compatibility report
description: Share a privacy-reviewed AirPods compatibility snapshot (Beats reports are exploratory).
title: "[Compatibility] "
labels:
  - compatibility
assignees:
  - raulgg
body:
  - type: markdown
    attributes:
      value: |
        Connect exactly one compatible AirPods or Beats device and run `airpods-control support-report`.
        Review the report locally before pasting or submitting it. Do not add a custom device name,
        serial number, Bluetooth/MAC address, account information, or raw system logs or dumps.

  - type: textarea
    id: report
    attributes:
      label: Compatibility report
      description: Paste the paste-ready GitHub report block generated by `airpods-control support-report`. The CLI prefills this field when the report fits safely in a URL.
      placeholder: |
        #### Device

        - Model: AirPods Pro 3
        - Model identifier: `BTHeadphones76,8231` (Bluetooth product ID 0x2027)
        - Device family: AirPods
        - macOS: 26.1.0
        - airpods-control: 0.1.0

        #### Capabilities

        - Advertised known listening modes: off, transparency, adaptive, noise-cancellation
        - Other advertised listening modes: none
        - Listening-mode query: answers with a recognized mode
        - Listening-mode setter: exposed (see write tests)
        - Conversation Awareness capability: supported
        - Conversation Awareness query: answers
        - Conversation Awareness setter: exposed (see write tests)

        #### Write tests

        - Status: not run
    validations:
      required: true

  - type: textarea
    id: notes
    attributes:
      label: Additional notes
      description: Add any other compatibility details that are safe to publish.

  - type: checkboxes
    id: privacy
    attributes:
      label: Privacy confirmation
      options:
        - label: I reviewed this report and confirmed it contains no custom device name, serial number, Bluetooth/MAC address, account information, or raw system logs or dumps.
          required: true
