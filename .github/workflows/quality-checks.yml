name: Quality Checks

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - stacked

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Stacked PRs run this workflow on every layer as if it targeted main.
  # Skip the macOS job on mid-stack layers; remaining bottom and top still
  # run. A skipped job reports Success for required checks.
  gate:
    name: Stack CI Gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      should-run: ${{ steps.gate.outputs.should-run }}
    steps:
      - name: Stack CI Gate
        id: gate
        uses: raulgg/stack-ci-gate@a0eedc1fcdb0de363849885126a562c4d46285dc # v1.0.1

  formatting:
    name: Check formatting
    needs: gate
    if: needs.gate.outputs.should-run == 'true'
    runs-on: macos-14
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install formatters
        uses: ./.github/actions/setup-mise
        with:
          install_args: swiftformat rumdl

      - name: Check formatting
        env:
          RUMDL_OUTPUT_FORMAT: github
        run: |
          swiftformat --version
          rumdl --version
          mise run format-check

      - name: Check Swift lint
        run: mise run swift-lint
